# STITCHCONT Tutorial Pointer

The canonical tutorial has moved out of the package source tree.

Use:

- `docs/stitchcont_tutorial.md` for the full CLI and interactive tutorial.
- `docs/CLI_REFERENCE.md` for the complete command-line flag reference.
- `docs/README.md` for the production documentation index.

This file is intentionally a pointer only, so release builds and source-tree browsing do not expose stale tutorial text.
